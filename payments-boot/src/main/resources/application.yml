server:
  port: 8081

logging:
  level:
    root: INFO
    org.springframework.cloud.function: DEBUG

spring:
  cloud:
    function:
      definition: payment-consumer;online-producer;offline-producer
    stream:
      kafka.bindings.payment-consumer-in-0.consumer.destinationIsPattern: true
      bindings:
        payment-consumer-in-0:
          destination: online|offline
          group: payment-processor
        online-producer-out-0:
          destination: online
        offline-producer-out-0:
          destination: offline




